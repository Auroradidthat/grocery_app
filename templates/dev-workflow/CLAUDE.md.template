# Project notes for Claude
<!--
  Copy this file to CLAUDE.md at the repo root and adjust the bracketed
  bits. Everything else can be used as-is — it's the process, not the
  project, that this file encodes. Add project-specific rules (styling
  preferences, testing conventions, deploy steps, etc.) as their own
  section at the bottom as they come up.
-->

## Documentation split
- `README.md` changelog: release and version history only (SemVer, `0.x`
  during the building stage).
- `DEVLOG.md`: per-session development notes, newest entry first.
Keep the two separate.

## Start of every session
Read the most recent (top) entry in `DEVLOG.md`. Briefly tell the user
where they left off and what the recorded **Next step** was, before doing
anything else.

## End of every session
Before ending, show the user the proposed DEVLOG entry and wait for
approval. Then commit it together with the rest of the session's work. Do
not commit the entry before it is approved.

Each entry uses these sections, concise but detailed enough to reconstruct
the session without memory:
- What I worked on (including mundane changes and fixes)
- Files / components changed
- Problems encountered and how they were resolved
- Decisions and reasons (technical or architectural, when known)
- Attempted / left unresolved
- Current state
- Next step (the specific thing to resume with)

## Git
- Work on `[DEFAULT BRANCH — e.g. main]` unless told otherwise; check
  `git branch --show-current` before committing.
- Never force-push. Confirm before pushing.

<!--
  Optional project-specific sections go below this line, e.g.:

  ## Styling
  - I write all CSS myself; explain and advise, don't write style rules.

  ## Testing
  - Run `[test command]` before committing; note failures in the DEVLOG
    entry rather than silently skipping them.

  ## Deploy
  - [however this project ships]
-->
